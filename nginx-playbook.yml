---
- name: installing and start nginx
  hosts: frontend
  become: true            # to install/start packages on EC2
  tasks:
    - name: installing nginx
      ansible.builtin.package:
        name: nginx
        state: present

    - name: start nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: true
