- name : loops playbook
  hosts: local
  connection: local
  tasks:
  - name : print names
    ansible.builtin.debug:
     msg: "Hello {{ item }}" # item is reserved
    loop:
    - Ramesh
    - suresh
    - Ram 